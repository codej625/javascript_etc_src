# 삼항연산자를 활용해보자!

```javascript
const val = 'ok';
const result = ( val === 'ok' ) ? true : false;

if (result) {
  // logic
} else {
  // logic
}
```

<br/>

```javascript
const a = 1;
const num = ( a > 0 ) ? true : false;

return num;
```